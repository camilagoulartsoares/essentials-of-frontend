const frutas = ['banana','pera','maca']


for (let i in frutas) {
    console.log(frutas[i])
}

const pessoa = {
    nome: 'Camila',
    age: 10

}

for (let chave in pessoa){
    console.log(pessoa[chave])
}